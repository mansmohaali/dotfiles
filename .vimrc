" Leader key


" Set compatibility to Vim only not Vi
set nocompatible

" Show line number
set number

" Status bar
set laststatus=2 

" Automaticaly wrap test that extends beyond the screen length
set wrap

" Encoding
set encoding=utf-8

" Set defaults clipboard to the system clipboard
set clipboard=unnamedplus

" Set line number
set number relaativenumber

" Set 
